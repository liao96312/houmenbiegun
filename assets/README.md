每个场景素材按这个目录放：

```text
assets/scenes/{scene_id}/
  poster.jpg
  bg_720p.mp4
  bg_480p.mp4
  noise.mp3
  opening_line.txt
  closing_line.txt
  scene_prompt.txt
```

一期当前先用 `data/scenes.json` 里的远程 poster 和浏览器生成白噪音。
